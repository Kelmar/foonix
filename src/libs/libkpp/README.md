This contains a small subset of the C++ STL.

This library is not a remotely complete C++ STL implementation, and might not even be fully standards compliant.

Those things that are not compliant aim to be missing rather than wrong however.

It is here to provide some STL like constructs for kernel development.  This is to make it easier to take portions
of the kernel and compile them in other platforms to do testing with.

